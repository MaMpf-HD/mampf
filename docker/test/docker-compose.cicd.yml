services:
  mampf:
    build:
      context: ../..
      dockerfile: docker/development/Dockerfile
      args:
        IN_CI_CD: "true"      
    image: mampf-test:cache
    volumes:
      - ${GITHUB_WORKSPACE}:/usr/src/app/
