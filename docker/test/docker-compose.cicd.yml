services:
  mampf:
    build:
      context: ../..
      dockerfile: docker/development/Dockerfile
      args:
        IN_CI_CD: "true"      
    image: mampf-test:cache
    volumes:
      - type: bind
        source: ${GITHUB_WORKSPACE}
        target: /usr/src/app/
