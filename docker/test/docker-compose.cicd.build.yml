services:
  mampf:
    build:
      context: ../..
      dockerfile: docker/development/Dockerfile
      args:
        IN_CI_CD: "true"
        RUBY_VERSION: ${RUBY_VERSION:?error}
    image: mampftest:image
