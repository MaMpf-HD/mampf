<div class="<%= container_class %>">
  <%= form.radio_button name, value, field_html_options %>
  <%= label_tag element_id, label, class: "form-check-label" %>
  <% if show_help_text? %>
    <span id="<%= help_text_id %>">
      <%= helpers.helpdesk(help_text, false) %>
    </span>
  <% end %>
</div>