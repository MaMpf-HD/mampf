<div class="card bg-light">
  <div class="card-header bg-generic-card-header">
    <h5>
      <%= t('basics.lectures') %>
    </h5>
  </div>
  <div class="card-body">
    <ul>
      <% lectures.each do |l| %>
        <li>
          <% if l.content_accessible_by?(current_user) %>
            <%= link_to l.title, l.path(current_user),
                        class: 'text-dark' %>
          <% else %>
            <span>
              <%= l.title %>
            </span>
          <% end %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
