<!-- Workflow Collapse -->
<div class="collapse mb-3" id="roster-workflow-collapse">
  <div class="card">
    <div class="card-body">
      <h5 class="card-title mb-3"><%= t('roster.roster_help.title') %></h5>
      <p class="text-muted mb-4"><%= t('roster.roster_help.intro') %></p>

      <!-- Phase 1: Choose Setup Method -->
      <div class="mb-4">
        <h6 class="fw-bold text-primary mb-3">
          <i class="bi bi-1-circle me-2"></i>
          <%= t('roster.roster_help.phase1.title') %>
        </h6>
        <div class="row g-3">
          <div class="col-md-6">
            <div class="border border-primary rounded p-3 h-100">
              <h6 class="fw-bold mb-2">
                <i class="bi bi-calendar-check me-2 text-primary"></i>
                <%= t('roster.roster_help.phase1.campaigns.title') %>
              </h6>
              <p class="small text-muted mb-2"><%= t('roster.roster_help.phase1.campaigns.description') %></p>
              <ul class="mb-0 small">
                <% t('roster.roster_help.phase1.campaigns.benefits').each do |benefit| %>
                  <li><%= benefit %></li>
                <% end %>
              </ul>
            </div>
          </div>

          <div class="col-md-6">
            <div class="border border-success rounded p-3 h-100">
              <h6 class="fw-bold mb-2">
                <i class="bi bi-x-circle me-2 text-success"></i>
                <%= t('roster.roster_help.phase1.skip_campaigns.title') %>
              </h6>
              <p class="small text-muted mb-2"><%= t('roster.roster_help.phase1.skip_campaigns.description') %></p>
              <ul class="mb-0 small">
                <% t('roster.roster_help.phase1.skip_campaigns.benefits').each do |benefit| %>
                  <li><%= benefit %></li>
                <% end %>
              </ul>
            </div>
          </div>
        </div>
      </div>

      <!-- Phase 2: What Happens Next -->
      <div class="mb-4 text-center">
        <div class="d-flex align-items-center justify-content-center mb-3">
          <div class="border-top flex-grow-1"></div>
          <i class="bi bi-arrow-down-circle mx-3 text-muted fs-4"></i>
          <div class="border-top flex-grow-1"></div>
        </div>
        <h6 class="fw-bold text-secondary mb-3">
          <i class="bi bi-2-circle me-2"></i>
          <%= t('roster.roster_help.phase2.title') %>
        </h6>
        <div class="row g-3">
          <div class="col-md-6">
            <div class="border border-primary rounded p-3">
              <div class="mb-2">
                <span class="badge bg-warning text-dark">
                  <i class="bi bi-lock me-1"></i>
                  <%= t('roster.roster_help.phase2.campaigns_during') %>
                </span>
              </div>
              <div class="text-center fs-4 my-2 text-muted">↓</div>
              <div>
                <span class="badge bg-success">
                  <i class="bi bi-unlock me-1"></i>
                  <%= t('roster.roster_help.phase2.campaigns_after') %>
                </span>
              </div>
            </div>
          </div>

          <div class="col-md-6">
            <div class="border border-success rounded p-3">
              <span class="badge bg-success">
                <i class="bi bi-check-circle me-1"></i>
                <%= t('roster.roster_help.phase2.direct_always') %>
              </span>
            </div>
          </div>
        </div>
      </div>

      <!-- Phase 3: Available Anytime -->
      <div class="mb-3">
        <div class="d-flex align-items-center justify-content-center mb-3">
          <div class="border-top flex-grow-1"></div>
          <i class="bi bi-arrow-down-circle mx-3 text-muted fs-4"></i>
          <div class="border-top flex-grow-1"></div>
        </div>
        <h6 class="fw-bold text-success mb-3">
          <i class="bi bi-3-circle me-2"></i>
          <%= t('roster.roster_help.phase3.title') %>
        </h6>
        <div class="border border-success rounded p-3">
          <p class="small mb-2"><%= t('roster.roster_help.phase3.description') %></p>
          <ul class="mb-0 small">
            <% t('roster.roster_help.phase3.options').each do |option| %>
              <li><%= option %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Group Types Collapse -->
<div class="collapse mb-3" id="roster-group-types-collapse">
  <div class="card">
    <div class="card-body">
      <h5 class="card-title mb-3"><%= t('roster.roster_help.group_types.title') %></h5>
      <p class="text-muted mb-3"><%= t('roster.roster_help.group_types.intro') %></p>

      <div class="row g-3">
        <% unless component.lecture.seminar? %>
          <div class="col-md-6">
            <div class="border rounded p-3 h-100">
              <h6 class="fw-bold mb-2">
                <i class="bi bi-person-lines-fill me-2"></i>
                <%= t("registration.campaign.patterns.tutorials.title") %>
              </h6>
              <p class="mb-0 text-muted small">
                <%= t("registration.campaign.patterns.tutorials.description") %>
              </p>
            </div>
          </div>
        <% else %>
          <div class="col-md-6">
            <div class="border rounded p-3 h-100">
              <h6 class="fw-bold mb-2">
                <i class="bi bi-mic-fill me-2"></i>
                <%= t("registration.campaign.patterns.talks.title") %>
              </h6>
              <p class="mb-0 text-muted small">
                <%= t("registration.campaign.patterns.talks.description") %>
              </p>
            </div>
          </div>
        <% end %>

        <div class="col-md-6">
          <div class="border rounded p-3 h-100">
            <h6 class="fw-bold mb-2">
              <i class="bi bi-box-arrow-in-right me-2"></i>
              <%= t("registration.campaign.patterns.enrollment.title") %>
            </h6>
            <p class="mb-0 text-muted small">
              <%= t("registration.campaign.patterns.enrollment.description") %>
            </p>
          </div>
        </div>

        <div class="col-md-6">
          <div class="border rounded p-3 h-100">
            <h6 class="fw-bold mb-2">
              <i class="bi bi-clipboard-data me-2"></i>
              <%= t("registration.campaign.patterns.planning.title") %>
            </h6>
            <p class="mb-0 text-muted small">
              <%= t("registration.campaign.patterns.planning.description") %>
            </p>
          </div>
        </div>

        <div class="col-md-6">
          <div class="border rounded p-3 h-100">
            <h6 class="fw-bold mb-2">
              <i class="bi bi-collection me-2"></i>
              <%= t("registration.campaign.patterns.other.title") %>
            </h6>
            <p class="mb-0 text-muted small">
              <%= t("registration.campaign.patterns.other.description") %>
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<div class="card border-top-0">
  <% groups.each_with_index do |group, index| %>
    <div class="card-header bg-secondary text-white mt-3">
      <h6 class="mb-0 fw-bold"><%= t("registration.item.groups.#{group[:type]}") %></h6>
    </div>
    <div class="table-responsive">
      <table class="table table-hover align-middle mb-0">
        <thead class="table-light">
          <tr>
            <th><%= t('roster.columns.title') %></th>
            <% if group[:type] == :tutorials %>
              <th><%= t('roster.columns.tutors') %></th>
            <% elsif group[:type] == :talks %>
              <th><%= t('roster.columns.speakers') %></th>
            <% end %>
            <th><%= t('roster.columns.capacity') %></th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% group[:items].each do |item| %>
            <% campaign = component.active_campaign_for(item) %>
            <tr>
              <td class="fw-medium"><%= item.title %></td>
              <% if group[:type] == :tutorials %>
                <td><%= item.respond_to?(:tutors) ? item.tutors.map(&:name).join(", ") : "" %></td>
              <% elsif group[:type] == :talks %>
                <td><%= item.respond_to?(:speakers) ? item.speakers.map(&:name).join(", ") : "" %></td>
              <% end %>
              <td>
                <% if component.show_campaign_running_badge?(item, campaign) %>
                  <span class="badge bg-info text-dark">
                    <%= t('roster.campaign_running') %>
                  </span>
                <% else %>
                  <div class="d-flex align-items-center gap-2">
                    <% if item.capacity %>
                      <% overbooked = item.roster_entries.count > item.capacity %>
                      <div class="flex-grow-1" style="min-width: 60px;">
                        <%= progress_bar(
                              item.roster_entries.count,
                              item.capacity,
                              classification: :utilization,
                              height: "6px",
                              show_label: false,
                              container_class: "progress"
                            ) %>
                      </div>
                      <span class="small text-nowrap <%= 'text-danger fw-bold' if overbooked %>"><%= item.roster_entries.count %>/<%= item.capacity %></span>
                      <% if overbooked %>
                        <i class="bi bi-exclamation-triangle-fill text-danger"
                           data-bs-toggle="tooltip"
                           title="<%= t('roster.overbooked') %>"></i>
                      <% end %>
                    <% else %>
                      <span class="small text-muted"><%= item.roster_entries.count %></span>
                    <% end %>
                  </div>
                <% end %>
              </td>
              <td class="text-end">
                <div class="d-flex gap-2 justify-content-end align-items-center">
                  <% if !item.locked? %>
                    <%= link_to component.group_path(item),
                                class: "btn btn-sm btn-outline-primary",
                                title: t('roster.manage'),
                                data: { bs_toggle: "tooltip" } do %>
                      <i class="bi bi-person-lines-fill"></i>
                    <% end %>
                  <% elsif campaign %>
                    <%= link_to edit_lecture_path(component.lecture, tab: "campaigns", campaign_id: campaign.id),
                                class: "btn btn-sm btn-outline-secondary",
                                title: t('roster.view_campaign'),
                                data: { turbo_frame: "_top", bs_toggle: "tooltip" } do %>
                      <i class="bi bi-calendar-check"></i>
                    <% end %>
                  <% elsif !item.in_campaign? %>
                    <%= link_to edit_lecture_path(component.lecture, tab: "campaigns", new_campaign: true),
                                class: "btn btn-sm btn-outline-secondary",
                                title: t('roster.create_campaign'),
                                data: { turbo_frame: "_top", bs_toggle: "tooltip" } do %>
                      <i class="bi bi-calendar-plus"></i>
                    <% end %>
                  <% end %>

                  <% if item.skip_campaigns? %>
                    <% if item.can_unskip_campaigns? %>
                      <%= form_with model: item, scope: :rosterable,
                            url: send("#{item.class.name.underscore}_roster_path", item),
                            method: :patch,
                            data: { turbo: true, controller: "auto-submit-form" }, class: "d-flex align-items-center border border-secondary rounded-pill px-2 py-1" do |f| %>
                        <%= hidden_group_type_field(group_type) %>
                        <i class="bi bi-pencil me-2 text-muted" title="<%= t('roster.skip_campaigns') %>" data-bs-toggle="tooltip"></i>
                        <div class="form-check form-switch mb-0">
                          <%= f.check_box :skip_campaigns,
                                class: "form-check-input",
                                role: "switch",
                                id: dom_id(item, :skip_campaigns),
                                checked: true,
                                aria: { label: t('roster.skip_campaigns') },
                                data: { action: "change->auto-submit-form#submit" } %>
                        </div>
                      <% end %>
                    <% end %>
                  <% elsif item.can_skip_campaigns? %>
                    <%= form_with model: item, scope: :rosterable,
                          url: send("#{item.class.name.underscore}_roster_path", item),
                          method: :patch,
                          data: { turbo: true, controller: "auto-submit-form" }, class: "d-flex align-items-center border border-secondary rounded-pill px-2 py-1" do |f| %>
                      <%= hidden_group_type_field(group_type) %>
                      <i class="bi bi-pencil me-2 text-muted" title="<%= t('roster.skip_campaigns') %>" data-bs-toggle="tooltip"></i>
                      <div class="form-check form-switch mb-0">
                        <%= f.check_box :skip_campaigns,
                              class: "form-check-input",
                              role: "switch",
                              id: dom_id(item, :skip_campaigns),
                              checked: false,
                              aria: { label: t('roster.skip_campaigns') },
                              data: { action: "change->auto-submit-form#submit" } %>
                      </div>
                    <% end %>
                  <% end %>
                </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% end %>
</div>
