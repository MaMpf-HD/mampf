<% group_type = @rosterable.is_a?(Tutorial) ? :tutorials : :talks %>
<%= turbo_frame_tag "roster_maintenance_#{group_type}" do %>
  <%= render RosterDetailComponent.new(rosterable: @rosterable) %>
<% end %>
