<%= render(SubmissionCardComponent.new(sheet: @sheet, invitations: @invitations,
                                       partners: @partners, invited_users: @invited_users,
                                       error: @card_error)) %>
