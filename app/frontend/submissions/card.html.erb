<%= render(SubmissionCardComponent.new(sheet: @sheet, invites: @invites,
                                       partners: @partners, invited: @invited,
                                       error: @card_error)) %>
