<%= render partial: "user_registrations/first_come_first_served/select_multi_item",
           locals: { campaign: campaign,
                     disabled: local_assigns.fetch(:disabled, false),
                     items: items,
                     registered_items: local_assigns.fetch(:registered_items, []) } %>
