<tr>
  <% columns.each do |column| %>
    <th class="<%= column.css_class %>" data-mode="<%= column.data_mode %>">
      <% if column.label_hidden %>
        <span class="visually-hidden"><%= column.label %></span>
      <% else %>
        <%= column.label %>
      <% end %>

      <% if column.sublabel %>
        <br>
        <div class="text-muted"><%= column.sublabel %></div>
      <% end %>
    </th>
  <% end %>
</tr>
