<div class="<%= container_class %>">
  <%= form.radio_button name, value, radio_button_html_options %>
  <%= form.label html.label_for, label, class: "form-check-label" %>
  <% if show_help_text? %>
    <span id="<%= html.help_text_id %>">
      <%= helpers.helpdesk(help_text, false) %>
    </span>
  <% end %>
</div>