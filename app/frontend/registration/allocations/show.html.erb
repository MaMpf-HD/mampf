<% if @campaign.preference_based? && !@dashboard.allocation_run? %>
  <%= render partial: "registration/allocations/pre_allocation_demand",
             locals: { campaign: @campaign, dashboard: @dashboard } %>
<% else %>
  <%= render partial: "registration/allocations/dashboard",
             locals: { campaign: @campaign, dashboard: @dashboard } %>
<% end %>
