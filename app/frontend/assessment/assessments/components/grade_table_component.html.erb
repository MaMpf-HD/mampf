<% if any_displayed? %>
  <table class="table table-hover table-sm align-middle">
    <thead>
      <tr>
        <th><%= t("basics.name") %></th>
        <th class="text-center"><%= t("assessment.status") %></th>
        <th class="text-center" style="width: 1%; white-space: nowrap;">
          <%= t("assessment.grade") %>
        </th>
        <th><%= t("assessment.note") %></th>
        <% if show_grader_column? %>
          <th><%= t("assessment.graded_by") %></th>
        <% end %>
        <th><%= t("assessment.graded_at") %></th>
        <th></th>
      </tr>
    </thead>

    <tbody id="grading-table-body">
      <% displayed_participations.each do |participation| %>
        <%= render(GradeTalkRowComponent.new(
          participation: participation,
          talk: assessment.assessable
        )) %>
      <% end %>
    </tbody>
  </table>
<% else %>
  <div class="text-center text-muted py-3">
    <i class="bi bi-mortarboard me-1"></i>
    <%= t("assessment.no_grades_yet") %>
  </div>
<% end %>
