<%# Saves every row the marking-table controller collected as dirty, in one request. %>
<button
  type="button"
  class="btn btn-success btn-sm"
  disabled
  data-marking-table-target="bulkSave"
  data-action="click->marking-table#submitAll"
>
  <i class="bi bi-floppy-fill me-1"></i>
  <%= t("assessment.grading_tutorial.save_all") %>

  <span
    class="badge text-bg-light ms-1"
    hidden
    data-marking-table-target="dirtyCount"
  ></span>
</button>

<%= form_with url: url,
              method: :patch,
              local: true,
              data: { turbo: true, marking_table_target: "form" },
              html: { hidden: true } do %>
  <input
    type="hidden"
    name="<%= payload_name %>"
    data-marking-table-target="payload"
  >

  <% hidden_fields.each do |name, value| %>
    <input type="hidden" name="<%= name %>" value="<%= value %>">
  <% end %>
<% end %>
