<div class="card bg-light">
  <div class="card-header bg-generic-card-header">
    <h5>
      <%= t('basics.related_media') %>
      <span class="badge badge-secondary">
        <%= media.size + references.size %>
      </span>
    </h5>
  </div>
  <div class="card-body <%= from == 'tag' && questions ? 'card-medium' : 'card-large' %>">
    <div class="accordion"
         id="relatedMediaAccordion">
      <% media_hash = related_media_hash(references, media) %>
      <% media_hash.each do |sort, media_in_sort| %>
      <div class="card">
        <div class="card-header">
          <button class="btn btn-link"
                  type="button"
                  data-toggle="collapse"
                  data-target="#collapse-<%= sort %>">
            <%= Medium.sort_localized[sort] %>
            (<%= media_in_sort.size %>)
          </button>
        </div>
        <div id="collapse-<%= sort %>"
             class="collapse"
             data-parent="#relatedMediaAccordion">
          <div class="card-body">
            <div class="row">
              <% media_in_sort.each do |m| %>
                <div class="col-12 col-md-6 col-lg-4 col-xl-12 mb-3">
                    <%= render partial: 'media/medium',
                               locals: { medium: m.first,
                                         tags: false,
                                         from: 'tag',
                                         ref_link: m.second,
                                         lecture: nil } %>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      </div>
      <% end %>
    </div>
<!--     <div class="row">
      <% if references.any? %>
        <% references.each do |r| %>
          <div class="col-12 col-md-6 col-lg-4 col-xl-12 mb-3">
            <%= render partial: 'media/medium',
                       locals: { medium: r.medium,
                                 tags: false,
                                 from: 'tag',
                                 ref_link: r.manuscript_link,
                                 lecture: nil } %>
          </div>
        <% end %>
      <% end %>
      <% if media.any? %>
        <% media.each do |m| %>
          <div class="col-12 col-md-6 col-lg-4 col-xl-12 mb-3">
            <%= render partial: 'media/medium',
                       locals: { medium: m,
                                 tags: false,
                                 from: 'tag',
                                 ref_link: nil,
                                 lecture: nil } %>
          </div>
        <% end %>
      <% end %>
      <% if references.empty? && media.empty? %>
        <%= t('no_related_media') %>
      <% end %>
    </div> -->
  </div>
</div>