<%= render(SearchForm::SearchForm.new(url: tags_search_path, context: "tags")) do |c| %>
  <% c.add_tag_title_filter %>

  <% c.add_course_filter_with_edited_courses(current_user) %>

  <% c.add_per_page_filter(id: "tags_per") %>
  <% c.add_submit_field %>
<% end %>