<%= render(SearchForm::SearchForm.new(url: search_tags_path, context: "tags")) do |c| %>
  <% c.add_fulltext_filter %>

  <% c.add_course_filter_with_edited_courses(current_user) %>

  <% c.add_per_page_filter(id: "tags_per") %>
  <% c.add_submit_field %>
<% end %>