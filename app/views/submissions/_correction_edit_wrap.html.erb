<%= turbo_frame_tag "correction-#{submission.id}" do %>
  <%= render partial: "submissions/correction_edit",
              locals: { submission: submission, errors: local_assigns[:errors] } %>
<% end %>
