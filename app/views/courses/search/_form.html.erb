<!-- filepath: /home/denis/mampf/app/views/courses/search/_form.html.erb -->
<%= render(Search::FormComponent.new(
  url: search_courses_path,
  scope: 'search',
  remote: true,
  method: :get
)) do |c| %>

  <% c.with_field(Search::Filters::EditorFilterComponent.new) %>

  <% c.with_field(Search::Filters::ProgramFilterComponent.new(context: 'course')) %>

  <% c.with_field(Search::Filters::FulltextFilterComponent.new) %>

  <% c.with_field(Search::Filters::PerPageFilterComponent.new(
    per_options: hits_per_page(true),
    default: default_hits_per_page(true)
  )) %>

  <% c.with_field(Search::Filters::TermIndependenceFilterComponent.new) %>

  <% c.with_footer(results_container_id: "course-search-results") %>
<% end %>