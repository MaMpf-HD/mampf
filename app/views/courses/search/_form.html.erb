<!-- filepath: /home/denis/mampf/app/views/courses/search/_form.html.erb -->
<%= render_search_form(url: search_courses_path, context: "courses") do |f|
  f.editor_filter
  f.program_filter
  f.fulltext_filter
  f.per_page_filter(
    per_options: hits_per_page(true),
    default: default_hits_per_page(true)
  )
  f.term_independence_filter
end %>