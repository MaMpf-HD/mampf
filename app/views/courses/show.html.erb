<% if @course.forum? %>
  <%= render partial: 'courses/forum_news',
           locals: { course: @course,
                     posts_count: @course.unread_forum_topics_count(current_user) } %>
<% end %>
<%= render partial: 'courses/infos',
           locals: { course: @course } %>
