<div class="card bg-green-lighten-4">
  <div class="card-header bg-green-darken-3">
    <h5 class="text-light">
      <%= tags_description %>
    </h5>
  </div>
  <div class="card-body">
    <% related_tags.sort_by(&:title).each do |t| %>
      <%= link_to t.title, tag_path(t), class: "badge badge-light" %>
    <% end %>
  </div>
</div>
