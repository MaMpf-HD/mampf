<% tags.each do |t| %>
  <% if inspection %>
    <%= link_to t.title, inspect_tag_path(t), class: "badge badge-light" %>
  <% else %>
    <%= link_to t.title, edit_tag_path(t), class: "badge badge-light" %>
  <% end %>
<% end %>
