<% tags.each do |t| %>
  <%= link_to t.short_title,
              edit_tag_path(t),
              class:'badge badge-light',
              data: { toggle: 'tooltip',
                      placement: 'top'},
              title: t.title %>
<% end %>