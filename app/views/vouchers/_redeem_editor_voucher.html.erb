<%= t('profile.become_editor', lecture: voucher.lecture.title) %>
<%= link_to redeem_voucher_path(params: { voucher_hash: voucher.secure_hash }),
            class: 'btn btn-primary',
            method: :post,
            remote: true do %>
  <%= t('profile.redeem_voucher') %>
<% end %>