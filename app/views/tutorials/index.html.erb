<% if @assignments.present? %>
<div data-controller="submission-table">
  <div class="row">
    <div class="col-12">
      <%= render partial: 'tutorials/header_index',
               locals: { assignment: @assignment,
                         tutorial: @tutorial,
                         stack: @stack } %>
    </div>
  </div>
  <div id="tutorial-table">
    <% if gradding_enabled?(@assignment) %>
      <%= render partial: 'tutorials/table_2',
               locals: { assignment: @assignment,
                         tutorial: @tutorial,
                         stack: @stack } %>
    <% else %>
      <%= render partial: 'tutorials/table',
               locals: { assignment: @assignment,
                         tutorial: @tutorial,
                         stack: @stack } %>
    <% end %>
  </div>
</div>
<% else %>
  <div class="jumbotron">
    <%= t('assignment.nothing_yet_in_lecture') %>
  </div>
<% end %>
<%= render partial: 'shared/generic_modal',
           locals: { sort: 'validateCertificate',
                     title: t('tutorial.certificate_check') } %>
