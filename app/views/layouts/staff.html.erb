<%# Edit mode for staff: the regular layout on a dotted background, without
    the "main" turbo frame, so that sidebar links leave the edit mode. %>
<% content_for :staff_edit_mode, "true" %>
<%= render template: "layouts/application" %>
