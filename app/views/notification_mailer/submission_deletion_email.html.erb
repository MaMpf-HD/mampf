<%= t('mailer.submission_deletion_text_html',
      deletion_date: I18n.l(@deletion_date)) %>
<p>
  <%= t('mailer.submission_deletion_lectures') %>
  <ul>
    <% @lectures.each do |l| %>
      <li>
        <%= l.title %>
      </li>
    <% end %>
  </ul>
</p>