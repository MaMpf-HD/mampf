<%= t('mailer.new_editor', title: @lecture.title_with_teacher) %>
<br>
<!--- <%= t('mailer.subscribe_editor') %> --->
<p class="pt-2">
  <%= link_to(t('notifications.edit_lecture'),
              edit_profile_url,
              class: 'btn btn-primary') %>
</p>
